#!/usr/bin/env bash
set -o errexit
set -o nounset
set -o pipefail

if ! command -v ast-grep &> /dev/null; then
    echo "ast-grep is not installed. Install with: brew install ast-grep"
    exit 1
fi

ast-grep scan --threads 1 --globs '!target/**'
