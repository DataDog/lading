inputs:
  fuse_capture: scratch/captures/fuse_reads.jsonl
  blackhole_capture: scratch/captures/blackhole.jsonl
  lading_config: scripts/capture_test_happy.yaml

output_dir: scratch/captures/analysis

reconstruction: newline_delimited

checks:
  - completeness:
      min_ratio: 1.0
  - fabrication:
      max_count: 0
  - duplication:
      max_ratio: 0.0
