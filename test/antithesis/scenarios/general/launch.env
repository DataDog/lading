# Per-scenario launch settings for test/antithesis/bin/launch.sh.
SCENARIO_TEST_NAME="lading-general"
SCENARIO_DESCRIPTION="lading general: TCP generator pushing load at the sink oracle"
# The single system under test. sink (oracle) and workload (driver) are not faulted.
SCENARIO_FAULT_NODES="lading"
